apiVersion: v1
kind: Service
metadata:
  name: {{ include "olympus-service.fullname" . }}
  labels:
    {{- include "olympus-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "olympus-service.selectorLabels" . | nindent 4 }}
